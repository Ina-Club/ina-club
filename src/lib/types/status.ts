export enum GroupStatus {
  OPEN = "OPEN",
  CLOSED = "CLOSED",
  CANCELED = "CANCELED",
  EXPIRED = "EXPIRED",
  PENDING = "PENDING",
  PREVIEW = "PREVIEW",
}

export enum PaymentTokenStatus {
  ACTIVE = "ACTIVE",
  CONSUMED = "CONSUMED",
  RELEASED = "RELEASED",
}

export enum CouponStatus {
  ACTIVE = "ACTIVE",
  EXPIRED = "EXPIRED",
  USED = "USED",
}
